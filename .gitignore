# Demos produced by skills — kept locally, not committed
demo/

# Local env files with real API keys — never committed
.env
**/.env
!.env.example

# Node dependencies (adm-zip) inside the npx wrapper package
packages/get-skill/node_modules/

# Project-level Claude guidance — kept local to this checkout, not shared
CLAUDE.md

# Build artifacts (e.g. packaged skill zips from `package_skill.py`)
dist/

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*.lnk
.vscode/
.idea/
